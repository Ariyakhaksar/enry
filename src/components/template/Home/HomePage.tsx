import HeroHomePage from "@/components/modules/HeroHomePage";
import React from "react";

type Props = {};

const HomePage = (props: Props) => {
   return (
      <div>
         <HeroHomePage />
      </div>
   );
};

export default HomePage;
